from django.urls import path  # From django.urls import path for defining the path.
from . import views  # From current folder import views module so that we can reference our view functions


""" In urls module we gonna map urls to views or view function.
"""
